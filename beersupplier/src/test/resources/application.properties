spring.jpa.hibernate.ddl-auto=update

#RabbitMQ
queue.order=queue.order
fanout.removedBeer=fanout.removedBeer
fanout.queue.removedBeer=fanout.queue.removedBeer

# deactivate service registry for tests
spring.cloud.service-registry.auto-registration.enabled=false