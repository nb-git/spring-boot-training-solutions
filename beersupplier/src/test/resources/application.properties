spring.jpa.hibernate.ddl-auto=update

#RabbitMQ
queue.order=test.queue.order
fanout.removedBeer=test.fanout.removedBeer
fanout.queue.removedBeer=test.fanout.queue.removedBeer

# deactivate service registry for tests
spring.cloud.service-registry.auto-registration.enabled=false